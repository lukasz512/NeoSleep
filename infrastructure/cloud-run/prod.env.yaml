# Non-secret env vars for Cloud Run service neosleep-api-prod (NEO-45, ADR-025).
# Secrets are in Secret Manager — see secrets.list. Values mirror the Render service
# (neosleep-bff) as of 2026-09-25, split per environment.
NODE_ENV: production
DEFAULT_TENANT_SLUG: neosleep
FRONTEND_URL: https://pwa.neosleepcare.com
RESEND_FROM_EMAIL: noreply@mail.neosleepcare.com
RESEND_NOTIFY_TO: lukasz.ostrowski@neosleepcare.com
DB_POOL_MAX: "10"
