/**
 * Parses the sidebar collapsed state from localStorage.
 * Returns SIDEBAR_DEFAULT_COLLAPSED (false = expanded) when value is invalid.
 */
export declare function parseSidebarCollapsed(saved: string | null): boolean;
