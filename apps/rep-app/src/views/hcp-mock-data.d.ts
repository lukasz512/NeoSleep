/**
 * Mock HCP (Healthcare Professional) data for Contacts view.
 * Replace with API/BFF when backend is ready.
 */
export interface HCP {
    id: string;
    name: string;
    specialty: string;
    institution: string;
    region: string;
}
export declare const MOCK_HCPS: HCP[];
