# =============================================================================
# NeoSleep — Environment Variables (monorepo root)
# Copy to .env and fill in values. Never commit .env to git.
#
# BFF (services/api) reads these via: tsx --env-file=../../.env
# Vite (apps/*) reads VITE_* vars via envDir pointing to repo root.
# =============================================================================

# ─── Server ──────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=3000

# ─── Database ─────────────────────────────────────────────────────────────────
# Local dev (after: docker compose up -d)
DATABASE_URL=postgresql://neosleep:neosleep_local@localhost:5432/neosleep
# Production: postgresql://user:pass@your-db-host/dbname

# ─── Session ─────────────────────────────────────────────────────────────────
# Generate: openssl rand -hex 64
SESSION_SECRET=dev-secret-change-in-production

# ─── CORS / Origins ───────────────────────────────────────────────────────────
FRONTEND_URL=http://localhost:5173
BFF_ORIGIN=http://localhost:3000
OAUTH_REDIRECT_ORIGIN=http://localhost:5173

# ─── Google OAuth (OIDC) ─────────────────────────────────────────────────────
# Create at: console.cloud.google.com → APIs & Services → Credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:5173/auth/google/callback

# ─── Multi-tenancy ───────────────────────────────────────────────────────────
DEFAULT_TENANT_SLUG=neosleep

# ─── Admin seed ──────────────────────────────────────────────────────────────
# Used by scripts/seed-demo.ts to create the default admin account
ADMIN_DEFAULT_PASSWORD=change-me-in-production

# ─── Email (Gmail SMTP) ──────────────────────────────────────────────────────
# Use a Gmail App Password (not your account password)
# Guide: myaccount.google.com/apppasswords
GMAIL_USER=noreply@neosleepcare.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx
GMAIL_TO=admin@neosleepcare.com

# ─── Web Push / VAPID ────────────────────────────────────────────────────────
# Generate once per environment: npx web-push generate-vapid-keys
# WARNING: regenerating invalidates ALL existing push subscriptions.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@neosleepcare.com

# ─── Frontend (Vite — VITE_* vars are exposed to the browser bundle) ─────────
# BFF URL seen from the browser. In dev, leave empty (same-origin via Vite proxy).
VITE_API_URL=
# App version injected at build time by CI (e.g. VITE_APP_VERSION=$GIT_SHA)
VITE_APP_VERSION=dev
# Must match VAPID_PUBLIC_KEY above.
# In CI this is injected from per-environment GitHub secrets:
#   VITE_VAPID_PUBLIC_KEY_DEV / _UAT / _PROD → mapped to VITE_VAPID_PUBLIC_KEY at build time.
VITE_VAPID_PUBLIC_KEY=

# ─── Observability ───────────────────────────────────────────────────────────
# Set to 1 to persist console errors to tbl_console_errors in non-production
ENABLE_CONSOLE_LOG_DB=0
